A Spotify-Backend Clone

This is the spotify backend clone which contains some of the API that are used by spotify.

General API's like /register and /login are used which you can access by running this project in your system.

For Register API ---> localhost:3000/api/auth/register
For Login API ---> localhost:3000/api/auth/login
